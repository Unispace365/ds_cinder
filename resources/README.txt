All resources in this directory are compiled into the app.

They need to be defined in two places:

src/ds/app/FrameworkResources.h
vc10/FrameworkResources.rc

and then FrameworkResources.rc needs to be included in any application projects.